import React from 'react';
import { PageProps } from 'gatsby';
import ProjectList from '../../components/translation_workflow/ProjectList';
// import TranslationProject from '../components/translation_workflow/TranslationProject';

/**
 * Shows the teams translation projects
 * @param props PageProps
 */
const Translations = (props: PageProps) => <ProjectList />;

export default Translations;
